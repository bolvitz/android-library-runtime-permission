<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->
    <style name="Theme.PermissionFlow" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryContainer">@color/gray_900</item>
        <item name="colorOnPrimaryContainer">@color/gray_100</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/gray_700</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="colorSecondaryContainer">@color/gray_200</item>
        <item name="colorOnSecondaryContainer">@color/gray_900</item>

        <!-- Tertiary brand color -->
        <item name="colorTertiary">@color/gray_600</item>
        <item name="colorOnTertiary">@color/white</item>
        <item name="colorTertiaryContainer">@color/gray_300</item>
        <item name="colorOnTertiaryContainer">@color/gray_800</item>

        <!-- Background colors -->
        <item name="android:colorBackground">@color/white</item>
        <item name="colorOnBackground">@color/black</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/black</item>
        <item name="colorSurfaceVariant">@color/gray_100</item>
        <item name="colorOnSurfaceVariant">@color/gray_700</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Navigation bar -->
        <item name="android:navigationBarColor">@color/black</item>
    </style>

    <!-- Splash screen theme -->
    <style name="Theme.PermissionFlow.Splash" parent="Theme.PermissionFlow">
        <item name="android:windowBackground">@color/black</item>
    </style>
</resources>
